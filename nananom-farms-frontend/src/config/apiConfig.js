// src/config/apiConfig.js

// Define the base URL for your backend API
// During local development, this will typically be where your PHP server is running.
// For example: 'http://localhost:8000' if you're using `php -S localhost:8000 -t public`
// Or 'http://localhost/nananom-farms-backend/public' if you're using XAMPP/WAMP/MAMP with a virtual host/alias.
const API_BASE_URL = 'http://localhost:8000/api'; // Adjust this to your actual backend URL

export { API_BASE_URL };